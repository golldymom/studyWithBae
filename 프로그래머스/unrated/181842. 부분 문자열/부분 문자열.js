function solution(str1, str2) {
    var answer = '';
    str2.includes(str1) === true ? answer = 1 : answer = 0;
    // if(str2.includes(str1) === true){
    //     answer = 1
    // }
 return answer
}